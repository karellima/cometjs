#!/usr/bin/env node

"use strict";
var path     = require("path");
var fs       = require("fs");
var dirname  = path.join(path.dirname(fs.realpathSync(__filename)), '../');

require(dirname + 'comet.js').run();